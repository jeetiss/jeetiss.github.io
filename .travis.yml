language: node_js
node_js:
  - "10"

branches:
  only:
    - dev

install:
  - npm install

script:
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  keep-history: false
  target-branch: master
  local-dir: ./dist
  github-token: $GITHUB_TOKEN
  on:
    branch: dev
