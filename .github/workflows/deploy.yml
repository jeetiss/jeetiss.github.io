name: Build and Deploy

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - "*"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: igordcsouza/github-action-get-branch-name@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Log
        env:
          CONTEXT: ${{ env.BRANCH_NAME != null && format('{0}-{1}', 'review', env.BRANCH_NAME) }}
        run: echo "$CONTEXT"

      - name: Checkout
        uses: actions/checkout@v2

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          ACCESS_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: master
          FOLDER: dist
          TARGET_FOLDER: ${{ env.BRANCH_NAME != null && format('{0}-{1}', 'review', env.BRANCH_NAME) }}
          CLEAN: false
