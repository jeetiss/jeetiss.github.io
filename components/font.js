import Font from "next/font/local";
import { Inter as Constructor } from "next/font/google";

export const Inter = Constructor({ subsets: ["cyrillic"] });
export const Hlebozavod = Font({ src: "../public/Hlebozavod.woff" });
